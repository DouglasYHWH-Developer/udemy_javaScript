/**     EXERCICIO 05
 * Armazene a velocidade de um carro em uma variável, 
   com o número que desejar;
 * Faça uma estrutura if/else que verifica se ele está 
   acima da velocidade;
 * 80 é a velocidade máxima permitida;
 * Se estiver acima ou abaixo exiba mensagens console.log(;)
 */

const velocidade = 75;

if (velocidade > 80 ) {
  console.log("Levou multa");
} else {
  console.log("Não levou multa");
}
