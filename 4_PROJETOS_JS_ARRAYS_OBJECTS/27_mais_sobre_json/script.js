// Mais sobre JSON

// Rigoroso na estrutura, ou seja, você deve seqguir o pdrão imposto por este formato;
// Apenas aspas duplas;
//Não aceita comentários;

let pessoa = {
  nome: "Douglas",
  idade: 30,
};
console.log(pessoa.nome);
console.log(pessoa.idade);
