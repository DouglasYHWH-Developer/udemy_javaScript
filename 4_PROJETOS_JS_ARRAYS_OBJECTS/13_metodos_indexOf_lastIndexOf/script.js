// métodos de arrays
// indexOf: encontra o índice de um determinado elemento;
// lastIndexOf: encontra o último índice de um elemento;

let numeros = [0, 1, 3, 4, 5];

console.log(numeros.indexOf(5));
console.log(numeros.lastIndexOf(3));
