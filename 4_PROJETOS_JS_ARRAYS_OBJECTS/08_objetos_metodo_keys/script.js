// E também podemos verificar quais chaves cada objeto possui;
// Usando método keys();

let objetoA = {
  prop1: "teste",
  prop2: "testando",
};
console.log(objetoA);
console.log(Object.keys(objetoA));
